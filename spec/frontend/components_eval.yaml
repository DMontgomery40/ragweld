name: Evaluation Components
description: Evaluation UI components
version: "1.0"

components:
  - name: DatasetManager
    props:
      - name: repoId
        type: string
        required: true

  - name: EvalDrillDown
    props:
      - name: run
        type: EvalRun
        required: true

  - name: TraceViewer
    props: []
